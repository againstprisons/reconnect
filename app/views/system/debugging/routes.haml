.system-admin-debugging-routes
  %h1= title

  %a.pure-button.button{:href => '/system/debugging'}
    %i.fa.fa-chevron-left
    = t :back

  %ul
    - routes.each do |controller, routes|
      - routes.each do |routedesc|
        %li
          .y-margin-large
            %code.code-line
              &= routedesc[:path]

            = "=>"

            %code.code-line
              &= routedesc[:method]

            = "(#{[routedesc[:verbs]].flatten.join(", ")})"
