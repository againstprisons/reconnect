<h1>A new user has signed up</h1>

<p>
  <strong><%= name %></strong> has signed up to <%= site_name %>
  using the email address <code><%= email %></code>.
</p>

<%= helpers.button[userlink, "View new user"] %>

<p>
  If the above link does not work, copy and paste the following link into your
  browser:

  <a href="<%= userlink %>"><%= userlink %></a>
</p>

<p>This is an automated email from <%= site_name %>.</p>
